import Layout from '../../components/Layout'
import ProjectForm from '../../components/ProjectForm'

export default function ProjectsNew({ projects }) {
  return (
    <Layout>
      <ProjectForm />
    </Layout>
  )
}
